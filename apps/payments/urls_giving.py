"""
Giving-specific URLs (alias for /api/v1/giving/).
"""

from django.urls import path
from .views import PaymentViewSet

urlpatterns = [
    # Alias endpoints for /api/v1/giving/
    # These map to payment endpoints but with giving-specific naming
]






